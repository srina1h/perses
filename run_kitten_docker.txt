mkdir -p results logs custom-seeds

sudo docker run -it --init --cpuset-cpus="$(bash getcoreids.sh)" --name kit_1_norm \
  -v $(pwd)/results:/workspace/kitten/temp_testing_campaigns \
  -v $(pwd)/logs:/workspace/logs \
  -v $(pwd)/custom-seeds:/workspace/kitten/scripts/javascript/seeds:ro \
  kit_1_norm

  sudo docker buildx build --builder isolated-builder --cpuset-cpus="$(bash getcoreids.sh)" --load --build_arg=SEED_MODE="normal" -t kit_1_norm .