#include <stdio.h>
int main(void)
{
    for (int i = 0; i < 10; i++) {
      printf("hello\n");
    }
    // deleting a loop
    for (int i = 0; i < 10; i++) {
      printf("hello\n");
    }
    int a = 1;
    int d = 1;
    int b = 2;
    int c = 3;
    // one more function call on the same line
    printf("hello\n");printf("hello\n");printf("hello\n");printf("%d\n", a + a);
    // add extra code.
    printf("extra");
    printf("extra");
    return 0;
}
